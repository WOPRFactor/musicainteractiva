# ============================================================================
# EMION · Backend (Flask) — dependencias
# ============================================================================

# --- Núcleo ---
Flask==3.1.0
Werkzeug==3.1.3
python-dotenv==1.0.1
gunicorn==23.0.0
gevent==24.11.1

# --- Datos ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Autenticación ---
Flask-JWT-Extended==4.7.1
flask-bcrypt==1.0.1

# --- Tiempo real (clases en vivo / chat — se cablea al construir esa fase) ---
Flask-SocketIO==5.4.1
python-socketio==5.11.4

# --- Tareas async / programadas (procesar video, emails — fase posterior) ---
celery==5.4.0
redis==5.2.1
APScheduler==3.11.0

# --- Infra web ---
Flask-Cors==5.0.0
Flask-Limiter==3.9.2
